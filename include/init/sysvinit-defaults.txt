# Path to Proxy.py.
#DIR=/opt/SwiperProxy/swiperproxy

# Run SwiperProxy as username.
#DAEMON_USER=swiperproxy

# Run SwiperProxy with the following arguments.
#DAEMON_OPTS="-c /etc/swiperproxy/proxy.conf"

# Path to pidfile.
#PIDFILE=/var/run/swiperproxy.pid
